<%= form_with model: @passenger, class: '_form__container' do |f| %>
  <p class="_form_instruction">Please provide the following information to <%= action_name %> the passengers to our database:</p>

  <% if @passenger.errors.any? %> 
    <ul class="errors"> 
      <% @passenger.errors.each do |column, message| %> 
        <li class="red">
          <strong><%= column.capitalize %></strong> <%= message %>
        </li>
      <% end %>
    </ul>
  <% end %>

  <div class="_form-text-container">
    <div class="_form">
      <%= f.label :name, class: "_form__label" %>
      <%= f.text_field :name, class: "_form__text_field" %>
    </div>

    <div class="_form">
      <%= f.label :phone_num, class: "_form__label" %>
      <%= f.text_field :phone_num, class: "_form__text_field" %>
    </div>
  </div>

  <%= f.submit action_name, class: "_form__passenger-btn" %>
<% end %>